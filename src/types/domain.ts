export type DomainFields = {
  _id: string
  title: string
  description: string
  isActive: boolean
  isDeleted: boolean
  createdAt: string
}

export type DomainFormFields = {
  title: string
  description: string
}
